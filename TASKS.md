# MILLer — Prioritised Action List

> Ranked by **impact × urgency**. Each task references findings in `AUDIT.md` (§VII) and `ISSUES.md`.
> Estimates assume a single experienced Rust/Python developer.
> Generated by TABULA RASA v2 audit on 2026-05-04.
> Previous tasks T-01 through T-46 are all resolved — see CHANGELOG.md for details.

---

## 🔴 CRITICAL — Do These Now

### T-47 · Fix 4 Ops With PE Engine but No ANEC Converter

- **ISSUES ref**: I-21
- **AUDIT ref**: §II-A, §IV (B-1 through B-4)
- **Severity**: CRITICAL
- **Effort**: S (0.5 day)
- **Detail**: Move `MILSliceUpdate`, `MILReverse`, `MILSlidingWindows`, `MILArgsort` from `Some(AneEngine::PE)` to `None` in `default_engine()`. Add `"slice_update"`, `"reverse"`, `"sliding_windows"`, `"argsort"` to `CPU_ONLY_OPS` set. These ops pass placement validation as ANE-legal but map to `MirOpCompat::Unsupported` at emission time, causing silent CPU fallback with sync stalls.

### T-48 · Fix Palettize Weights No-Op

- **ISSUES ref**: I-22
- **AUDIT ref**: §II-E, §IV (B-5)
- **Severity**: CRITICAL
- **Effort**: M (1 day)
- **Detail**: The `palettize_weights` pass computes `bits` values but discards them with `_ = (weight, bits)`. No palette annotation is actually emitted — the pass is dead code. Wire `bits` into weight annotation. Add bit-width validation ensuring `bits ∈ {1,2,3,4,6,8}` (reject 5-bit and 7-bit widths). The `attention_bits + 2` computation can produce 6 (valid) or 7 (invalid) for Q/K conservative mode.

### T-49 · Add ~30 Missing Ops to CPU_ONLY_OPS

- **ISSUES ref**: I-23
- **AUDIT ref**: §II-B
- **Severity**: HIGH
- **Effort**: S (0.5 day)
- **Detail**: The `CPU_ONLY_OPS` HashSet is missing entries from the canonical CPU-only list. Add: `for`, `call`, `condition`, `yield`, `return`, `shape`, `rank`, `size`, `dimension_size`, `is_finite`, `is_infinite`, `is_nan`, `negative`, `reciprocal`, `reverse_square_root`, `rint`, `signbit`, `strided_slice_update`, `one_hot`, `dequantize_lut`, `extract`, `from_elements`, `func`, `get_coordinates`, `local_convolution`, `lp_norm`, `prune`, `pruning_metric`, `pruning_structure`, `variable_from_tensor`, `assign_variable`, `placeholder`, `device_hint`, `nf`, `unrealized_fold`, `col_to_im`, `create_texture_tensor`, `dynamic_shape_cast`, `reinterpret_cast`, `sparse_tensor_storage`, `materialize_sparse_tensor`, `buffer_tensor`. These provide defense-in-depth against future engine assignment drift.

---

## 🟠 HIGH — Do These Next Sprint

### T-50 · Fix Broadcast FP16-Only to Include A13

- **ISSUES ref**: I-24
- **AUDIT ref**: §II-C, §IV (B-6)
- **Severity**: HIGH
- **Effort**: S (0.5 day)
- **Detail**: `broadcast_fp16_only()` currently matches `A11Legacy | A12` but the ANE constraint canon says A13 also requires FP16-only broadcast (A13 uses A14Minus elementwise converters). Change to `matches!(self, A11Legacy | A12 | A13)`. Update test `test_broadcast_fp32_allowed_on_a13` to expect rejection.

### T-51 · Add ReduceMin Non-FP Dtype Guard

- **ISSUES ref**: I-25
- **AUDIT ref**: §II-C, §IV (B-7)
- **Severity**: HIGH
- **Effort**: S (0.5 day)
- **Detail**: Add a `MILReduceMin` match arm in `validate_placement_with_context()` that checks `target_family.supports_reducemin_all_dtypes()` when `ctx.dtype` is a non-FP type (Int8, UInt8, Int16). On A11/A12/A13, ReduceMin with non-FP dtypes will pass validation but fail at ANE runtime.

### T-52 · Fix E4M3 Support for A17 Pro (V11)

- **ISSUES ref**: I-26
- **AUDIT ref**: §II-C, §IV (B-8)
- **Severity**: HIGH
- **Effort**: M (1 day)
- **Detail**: V11 (A17 Pro) maps to `AneFamily::A16` which doesn't support E4M3. The canon says E4M3 is supported on A17+ (LSE_6). Options: (a) add `AneFamily::A17` variant mapping V11→A17, or (b) add revision-level override in `supports_e4m3()` for V11, or (c) extend `supports_e4m3()` to `matches!(self, A16 | A18)` if A16 silicon supports E4M3. Option (a) is cleanest but requires updating all family-dependent logic.

### T-53 · Wire `validate_tensor_dims()` Into Placement Pipeline

- **ISSUES ref**: I-27
- **AUDIT ref**: §II-D, §IV (B-10)
- **Severity**: HIGH
- **Effort**: S (0.5 day)
- **Detail**: `AneHwLimits::validate_tensor_dims()` exists in `ane_hw_limits.rs:148-193` but is never called from `validate_placement_with_context()`. Hardware tensor dimension limits (max_tensor_width, max_tensor_height, etc.) are defined but not enforced at placement time, meaning oversized tensors pass validation but fail at ANE runtime.

### T-54 · Replace `panic!()` in Emission and Lowering Code

- **ISSUES ref**: I-28
- **AUDIT ref**: §III (CQ-1, CQ-2, CQ-4)
- **Severity**: HIGH
- **Effort**: S (0.5 day)
- **Detail**: Replace 7 `panic!()` calls in production code paths: 4 in `mir_to_proto.rs:865,877,994,1004` (unexpected weight format), 3 in `mil_lower.rs:943,1412,3320` (unexpected AIR op patterns), and 2 in `legality_rewrite.rs:3903,4271` (Select/Where/Tile passthrough). Replace with `anyhow::bail!()` returning proper errors.

### T-55 · Replace `.unwrap()` in Weights File I/O

- **ISSUES ref**: I-29
- **AUDIT ref**: §III (CQ-3)
- **Severity**: HIGH
- **Effort**: M (1 day)
- **Detail**: ~20 `.unwrap()` calls in `weights.rs:530-652` in the binary format parsing/writing path. A malformed weight file crashes the compiler instead of returning an error. Replace with `?` operator for Result propagation.

### T-56 · Remove Qwen3 Default for ModelArchConfig

- **ISSUES ref**: I-30
- **AUDIT ref**: §III (CQ-7)
- **Severity**: HIGH
- **Effort**: S (0.5 day)
- **Detail**: `ModelArchConfig::default()` hardcodes Qwen3-0.6B dimensions (vocab_size=151936, embed_dim=1024, etc.). Any caller using `default()` gets Qwen3 assumptions silently. Remove `Default` impl or rename to `qwen3_0_6b()` factory method. Force callers to provide explicit config.

### T-57 · Fix Qwen3 Architecture Fallback in Bridge

- **ISSUES ref**: I-31
- **AUDIT ref**: §III (CQ-8, CQ-9)
- **Severity**: HIGH
- **Effort**: S (0.5 day)
- **Detail**: `mir_to_compat.rs:455` falls back to Qwen3 architecture when none specified: `let arch = architecture.cloned().unwrap_or(ModelArchitecture::Qwen3)`. `shape_inference.rs:72,566` defaults to 32768 max_seq_len (Qwen3-0.6B). Return error when no architecture is provided instead of silently defaulting.

### T-58 · Add Tests for ir::payload, ir::shard_desc, ir::serialize

- **ISSUES ref**: I-32
- **AUDIT ref**: §V
- **Severity**: HIGH
- **Effort**: L (3 days)
- **Detail**: Three modules with 0% test coverage and 30 pub fn total. `payload.rs` has 16 untested pub fn (all 5 family payload types, `from_spec`, `from_spec_with_override`, `to_json`). `shard_desc.rs` has 6 untested pub fn (`sharded_pipeline_shards`, `lower_shard_to_mir`, `build_sharded_pipeline_pir`). `serialize.rs` has 8 untested pub fn (round-trip for SIR/AIR/MIR/PIR). The precision adaptation pipeline (`from_spec_with_override`) is completely untested end-to-end.

### T-59 · Add Tests for lab::session, lab::harness, lab::fallback

- **ISSUES ref**: I-33
- **AUDIT ref**: §V
- **Severity**: HIGH
- **Effort**: L (2 days)
- **Detail**: Three 0%-coverage critical modules. `session.rs` has 7 untested pub fn (task hashing, knowledge update, artifact manifest). `harness.rs` has 14 untested pub fn (LabRunBuilder, LabRun, all builder paths). `fallback.rs` has 3 untested pub fn (FallbackDetector::detect_from_timing). These are the lab's main orchestration entry points.

---

## 🟡 MEDIUM — Do These Soon

### T-60 · Fix Tile Decomposition Placeholder Zeros

- **ISSUES ref**: I-34
- **AUDIT ref**: §IV (B-9)
- **Severity**: MEDIUM
- **Effort**: S (0.5 day)
- **Detail**: Tile decomposition in `legality_rewrite.rs:542-543` generates reshape_shape with `0` placeholders. `resolve_reshape_zeros()` uses batch=1 heuristic for multi-zero resolution, which is incorrect for general Tile patterns. Use ctx dimensions when available; document that ctx is required for correct Tile decomposition.

### T-61 · Add Cross-Validation Test for Python vs Rust Emission

- **ISSUES ref**: I-35
- **AUDIT ref**: §III (D-1, D-4)
- **Severity**: MEDIUM
- **Effort**: M (1 day)
- **Detail**: Python bridge (coremltools subprocess) and Rust proto-direct path exist independently with no cross-validation. Create a structural equivalence test that emits the same MIR graph through both paths and verifies identical MIL topology. Document which ops are supported by each path.

### T-62 · Fix Conv Constraint Discarded Params

- **ISSUES ref**: I-36
- **AUDIT ref**: §II-D
- **Severity**: MEDIUM
- **Effort**: S (0.5 day)
- **Detail**: `validate_conv_constraints()` takes `kernel_d` and `stride` params but discards them with `let _ = (kernel_d, stride)`. Implement depth dimension and stride validation per the constraint docs (power-of-2 kernel dimensions, stride=1 for batch/channel).

### T-63 · Fix Zero-Channels Interleave Bypass

- **ISSUES ref**: I-37
- **AUDIT ref**: §II-D
- **Severity**: MEDIUM
- **Effort**: S (0.5 day)
- **Detail**: `channels.unwrap_or(0)` in `placement_validate.rs:244` trivially passes interleave divisibility check because zero is divisible by any factor. Return `AneConditional` or warning when channels are unknown rather than silently allowing.

### T-64 · Centralize Palette Bit-Width Validation

- **ISSUES ref**: I-38
- **AUDIT ref**: §II-E (A-13)
- **Severity**: MEDIUM
- **Effort**: S (0.5 day)
- **Detail**: Palette bit-width validation {1,2,3,4,6,8} appears in 3 places: `lut_projection.rs:151`, `task_spec.rs:937`, and `sir.rs:1087` (doc comment). Create `ane_ir::ane_layout::validate_palette_bits()` and call from all sites.

### T-65 · Unify CPU-Only Classification

- **ISSUES ref**: I-39
- **AUDIT ref**: §III (E-1)
- **Severity**: MEDIUM
- **Effort**: M (1 day)
- **Detail**: CPU-only op classification is maintained in two places: `CPU_ONLY_OPS` HashSet in `cpu_only_ops.rs` and `default_engine() → None` branch in `mir.rs`. Derive the CPU-only set from `default_engine() == None` to maintain a single source of truth.

### T-66 · Add Remaining MirOpCompat Variants

- **ISSUES ref**: I-40
- **AUDIT ref**: §II-A (resolved ops note)
- **Severity**: MEDIUM
- **Effort**: M (2 days)
- **Detail**: Ops with real ANEC converters that still map to `MirOpCompat::Unsupported`: BatchNorm, InstanceNorm, L2Norm, MaxPool, AvgPool, L2Pool, Quantize, Dequantize, Resize/Resample variants, CropResize, DepthToSpace, SpaceToDepth, PixelShuffle, PixelUnshuffle, BatchToSpace, SpaceToBatch, and others. These have ANE hardware support but lack proto emission code.

---

## Task → Issue Cross-Reference

| Task | Issue(s) | Severity | Effort | Status |
|------|----------|----------|--------|--------|
| T-47 | I-21 | CRITICAL | S | ⬜ |
| T-48 | I-22 | CRITICAL | M | ⬜ |
| T-49 | I-23 | HIGH | S | ⬜ |
| T-50 | I-24 | HIGH | S | ⬜ |
| T-51 | I-25 | HIGH | S | ⬜ |
| T-52 | I-26 | HIGH | M | ⬜ |
| T-53 | I-27 | HIGH | S | ⬜ |
| T-54 | I-28 | HIGH | S | ⬜ |
| T-55 | I-29 | HIGH | M | ⬜ |
| T-56 | I-30 | HIGH | S | ⬜ |
| T-57 | I-31 | HIGH | S | ⬜ |
| T-58 | I-32 | HIGH | L | ⬜ |
| T-59 | I-33 | HIGH | L | ⬜ |
| T-60 | I-34 | MEDIUM | S | ⬜ |
| T-61 | I-35 | MEDIUM | M | ⬜ |
| T-62 | I-36 | MEDIUM | S | ⬜ |
| T-63 | I-37 | MEDIUM | S | ⬜ |
| T-64 | I-38 | MEDIUM | S | ⬜ |
| T-65 | I-39 | MEDIUM | M | ⬜ |
| T-66 | I-40 | MEDIUM | M | ⬜ |

---

## Archived Tasks (All Resolved)

Tasks T-01 through T-46 from the v1 audit are all resolved. See `CHANGELOG.md` for details.
